# Agent OJ

`agent-oj` 是一个围绕算法题、代码提交、代码评测与 AI 辅助问答构建的单一上下文系统。

## Language

**程序题评测契约**:
程序题的提交、执行与判定统一基于标准输入和标准输出，而不是函数名和参数调用。
_Avoid_: 函数级执行契约, `functionName + testInputs`

**题目本体**:
题目本体只表达语言无关的题意、输入输出规则、样例与判题规则，不承载具体语言代码结构。
_Avoid_: 语言模板容器, 函数签名定义

**题目语言资产**:
题目语言资产是某道题在某种编程语言下的起始代码、参考解答与编译运行配置集合。
_Avoid_: 共享题目字段, 题目本体字段

**试运行**:
试运行是用户基于题面公开的示例用例对当前代码做的即时执行，不代表正式判题结果。
_Avoid_: 正式提交, 真实评测

**同步试运行**:
同步试运行是在用户发起试运行请求后由接口直接完成执行并立即返回结果的交互方式，不创建后台判题任务，也不形成提交记录。
_Avoid_: 异步提交, 提交留档

**全量试运行**:
全量试运行是指试运行会执行全部公开用例并返回逐用例明细，即使中途出现 WA 或执行失败也不短路。
_Avoid_: 试运行首错即停, 只返回部分公开用例结果

**正式提交**:
正式提交是用户代码针对完整判题集执行的真实评测流程，产出最终判定结果。
_Avoid_: 试运行, 示例验证

**示例用例**:
示例用例是展示在题面上的公开输入输出样例，同时也是试运行默认使用的测试数据。
_Avoid_: 完整判题集, 隐藏用例

**完整判题集**:
完整判题集是正式提交时用于真实评测的全量测试数据，默认不向用户公开。
_Avoid_: 示例用例, 公开样例

**判题方式**:
判题方式定义程序题结果如何被判定，例如标准输出比对、特判或交互判题。
_Avoid_: 语言模板, 代码风格

**判题资产**:
判题资产是服务于程序题评测的数据与程序集合，包括示例用例、完整判题集以及按需提供的特判程序或交互器。
_Avoid_: 题目语言资产, 前端展示模型

**特判程序**:
特判程序是某道题在 SPJ 判题方式下用于解释用户输出是否正确的固定判题资产，创建后应被版本化和审计。
_Avoid_: 语言模板, 动态生成答案

**标准流用例**:
标准流用例是由输入文本和期望输出文本构成的判题单元，遵循标准输入和标准输出契约。
_Avoid_: 参数对象用例, 函数返回值用例

**用例序号**:
用例序号是标准流用例在某个题目版本内的稳定身份标识与执行顺序标识，发布后不可在同版本内重排或复用。
_Avoid_: 运行时临时顺序, 同版本内重排

**公开前置顺序**:
公开前置顺序是完整判题集中用例排列的约束：同一题目版本内，所有公开用例必须先于所有隐藏用例出现。
_Avoid_: 隐藏用例插在公开用例之前, 公开隐藏交错排序

**标准比对判题**:
标准比对判题通过比较用户程序标准输出与期望输出文本来决定结果，不依赖特判程序；在答案匹配层面只产生 AC 或 WA，不引入 PE。
_Avoid_: 特判, 交互判题, PE

**最小文本归一化**:
最小文本归一化是标准比对判题在比较前唯一允许的文本整理规则：统一换行符为 `\n`，并忽略文件末尾单个换行差异；除此之外任何字符差异都视为不同。
_Avoid_: `trim`, token 比较, 无序容忍, 隐式宽松匹配

**执行终态**:
执行终态描述提交源码在编译和运行阶段的实际结束状态，例如 CE、TLE、MLE、RE、SE，它与答案匹配层面的 AC、WA 不同。
_Avoid_: 仅用 WA 表达一切失败

**平台侧失败**:
平台侧失败是指评测基础设施、判题参考程序、特判程序或平台内部编排自身发生异常而导致评测无法按预期完成的情况，对外统一归为 SE。
_Avoid_: 把用户程序异常记成系统错误

**编译阶段失败**:
编译阶段失败是提交源码在进入任何用例执行前因编译错误而终止的情况，属于执行终态中的 CE；此时不产生逐用例执行明细。
_Avoid_: 伪造成逐用例 CE, 把未执行用例视为已执行

**通过制判题**:
通过制判题只关心题目是否通过，不引入分值、子任务分组或部分分体系。
_Avoid_: OI 记分制, 部分分

**固定判题数据**:
固定判题数据是正式提交时直接使用并持久化保存的标准输入与标准输出集合，而不是在判题时临时依赖参考解答动态生成。
_Avoid_: 动态生成标准答案, 运行时依赖参考解答

**判题包**:
判题包是某个题目版本在绑定的默认执行环境版本下，通过发布闸门后生成的不可变正式评测运行包，固化引用完整判题集顺序、标准答案、判题方式、特判程序、资源限制与语言运行配置等资产。
_Avoid_: 正式提交时临时拼装资产, 运行时多表散读

**历史评测保留**:
历史评测保留是指只要某个题目版本已经产生过提交记录，其绑定的判题包与执行环境版本就必须持续可用，以支持历史结果追溯、审计与后台核查。
_Avoid_: 回收历史判题包, 覆盖历史执行环境, 断开历史追溯链路

**语言参考解答**:
语言参考解答是某种语言下提供给用户学习、给 AI 讲解或给代码模板生成参考的正确实现，不参与正式提交判题。
_Avoid_: 判题真相源, 正式评测依赖

**判题参考程序**:
判题参考程序是题目制作阶段用于生成或校验固定判题数据的标准程序，不属于正式提交链路，并固定使用平台内部单一语言实现。
_Avoid_: 语言参考解答, 用户提交程序, 跟随支持语言复制多份

**答案标准输出**:
答案标准输出是由判题参考程序在制题阶段为某个标准流用例生成并固化的标准输出文本。
_Avoid_: 运行时动态生成答案

**特判结果合同**:
特判程序在成功执行时通过标准输出返回结构化判定结果，而非仅靠退出码表达 AC 或 WA；非零退出码表示特判程序自身失败。
_Avoid_: 仅用退出码表达业务判定

**特判判定消息**:
特判判定消息是特判程序在返回业务判定时可附带的短文本说明，用于帮助用户理解 AC 或 WA 的原因，但必须受错误信息可见性约束。
_Avoid_: 回传隐藏答案细节, 输出冗长调试日志

**特判结构化输出**:
特判结构化输出是特判程序成功执行时写到标准输出的固定 JSON 对象，只允许包含必填的 `verdict` 与选填的 `message` 两个业务字段。
_Avoid_: 自由文本输出, 自定义业务字段, 多行混合日志

**SPJ 自验样例**:
SPJ 自验样例是用于验证特判程序自身正确性的固定样例，包含给定 `stdin`、答案标准输出与预设 `userStdout` 后应得到的结构化判定结果。
_Avoid_: 只验用户解不验特判程序, 无法自动验证的 SPJ

**错误实现样例**:
错误实现样例是题目制作阶段用于验证判题数据或特判程序具备拦错能力的故意错误程序样例，必须在完整判题集上无法得到 AC。
_Avoid_: 只验证正确解, 无法证明判题能拦错

**自动发布**:
自动发布是题目在通过系统校验后无需人工审核即可进入可用状态的发布方式。
_Avoid_: 人工审核发布, 手工上架

**题目公开范围**:
题目公开范围是题库题目对外可见、可检索与可进入的范围约束，可区分为创建者私有可用、非公开可用与全站公开。
_Avoid_: 发布即全站公开, 可用性与公开性混为一体

**公共题库上架**:
公共题库上架是后台受权角色将已可用的题库题目显式开放到全站公共检索范围的动作。
_Avoid_: 自动发布后直接进入公共题库

**题目归档**:
题目归档是将题库题目从普通用户的新会话入口与公共检索范围中移除，但保留其历史版本、既有会话、提交记录与后台追溯能力的生命周期动作。
_Avoid_: 物理删除题目历史, 切断提交追溯链路

**发布闸门**:
发布闸门是题目进入自动发布前必须全部通过的一组系统校验，包括判题资产、固定判题数据与至少一门语言资产的可运行性验证。
_Avoid_: 无校验发布, 仅人工感知检查

**题目版本**:
题目版本是题目在某一时刻的不可变快照，包含题目本体、判题资产、固定判题数据与语言资产。
_Avoid_: 原地覆盖线上题目

**题库题目**:
题库题目是平台中可被检索、选取、绑定会话并承载多个题目版本及其草稿尝试序列的长期题目对象，不是一次性会话临时产物。
_Avoid_: 会话临时题, 一次性生成物

**版本绑定会话**:
版本绑定会话是用户进入题目编辑态后与某个题目版本建立的显式关联，试运行与正式提交都基于该版本执行。
_Avoid_: 隐式跟随最新版, 无版本上下文执行

**会话版本固定**:
会话版本固定是指用户会话一旦绑定某个题目版本，在会话结束或显式切换前，不会因为新版本发布而自动切换。
_Avoid_: 会话自动漂移到最新版, 用户无感换题义

**历史版本访问边界**:
历史版本访问边界是题目版本对外开放范围的约束：普通用户默认只能进入当前对外生效的题目版本，历史版本仅用于既有会话、提交记录与后台追溯。
_Avoid_: 普通用户随意新开历史版本, 长期并行多条公开题义

**管理员版本修订**:
管理员版本修订是后台受权管理员基于既有题库题目创建新题目版本的修题动作，用于调整题意、判题资产、语言资产或默认执行环境版本。
_Avoid_: 通过 workflow 原地改题, 普通用户直接改题

**支持语言集**:
支持语言集是某个题目版本允许用户试运行和正式提交的语言集合，只能由该版本已具备的题目语言资产构成。
_Avoid_: 全站默认语言, 未声明也可提交

**平台预设制题语言集**:
平台预设制题语言集是当前重构范围内单题 AI 制题默认要求完整生成并通过验收的固定语言集合，由平台统一配置，而不是由单次会话临时选择。
_Avoid_: 会话临时选语言, 单题制题时自由缩放语言范围

**草稿版本**:
草稿版本是尚未对用户可见的题目版本候选体，用于承载 AI 生成、系统校验与发布前准备工作。
_Avoid_: 半发布题目, 对外可见中间态

**失败草稿版本**:
失败草稿版本是未通过发布闸门但被保留的草稿版本，用于记录结构化失败原因并支持后续修复、重试与自动再验。
_Avoid_: 验题失败后直接丢弃, 无法追踪失败原因

**草稿尝试序列**:
草稿尝试序列是同一题库题目下按生成或修复尝试时间累积保留的一组草稿版本，新的尝试产生新的草稿版本，而不是覆盖旧草稿。
_Avoid_: 原地覆盖失败草稿, 丢失失败现场

**执行环境版本**:
执行环境版本是编译运行配置、语言命令模板、运行时版本与资源限制模板的可追溯快照，用于保证提交结果可复现。
_Avoid_: 隐式全局最新配置, 不可追溯运行环境

**默认执行环境版本**:
默认执行环境版本是某个题目版本对外默认采用的执行环境版本绑定，用户侧的试运行与正式提交默认都以它为准。
_Avoid_: 全局隐式最新环境, 题目版本不绑定运行时快照

**判题限制**:
判题限制是题目版本对时间、内存、输出等评测资源上限的业务声明，由执行环境版本负责落地执行。
_Avoid_: 仅依赖全局环境默认值

**语言限制系数**:
语言限制系数是执行环境版本针对不同语言附加在题目基准判题限制上的调整因子，用于平衡不同语言的运行开销。
_Avoid_: 所有语言绝对同限

**最终判定**:
最终判定是提交记录对外展示的单一主状态，统一表达 AC、WA、CE、TLE、MLE、RE、SE 等最终结果。
_Avoid_: 含混的 executeStatus, 同时暴露多套主状态

**短路判题**:
短路判题是在通过制正式提交中遇到首个非 AC 结果后立即停止后续用例执行的策略。
_Avoid_: 失败后仍继续全量执行

**通过计数**:
通过计数使用 `passCount / totalCount` 表达正式提交结果，其中 `totalCount` 是完整判题集总数，`passCount` 是短路停止前实际通过的用例数。
_Avoid_: 把 totalCount 理解为已执行用例数

**起始模板**:
起始模板是某种语言下提供给用户编辑的初始完整程序源码，必须语法合法且可编译可运行，但不要求通过题目。
_Avoid_: 空函数骨架, 不可执行片段

**语言资产验收**:
语言资产验收是某门支持语言进入发布闸门前必须通过的校验，至少包括语言参考解答全量通过以及起始模板可编译可运行。
_Avoid_: 仅校验参考解答, 仅校验模板

**公开用例**:
公开用例是会展示在题面上的判题用例，同时参与试运行与正式提交。
_Avoid_: 仅展示不参与正式判题的演示样例

**隐藏用例**:
隐藏用例是不向用户展示、仅在正式提交时参与判定的判题用例。
_Avoid_: 题面样例, 试运行用例

**试运行明细**:
试运行明细是逐个公开用例返回的执行反馈，包含输入、用户输出、执行终态、判定结果、资源消耗与错误信息。
_Avoid_: 仅返回总结果

**提交记录**:
提交记录是对正式提交及其真实评测结果的持久化留档，不包含试运行产生的即时结果。
_Avoid_: 试运行历史, 临时调试结果

**提交快照**:
提交快照是正式提交在创建时固化保存的评测输入快照，至少包括题目版本、执行环境版本、提交语言和完整源码文本，用于后续评测结果追溯与后台核查。
_Avoid_: 回读会话当前代码, 仅保存提交 ID 不保存源码

**提交可见明细**:
提交可见明细是正式提交对用户展示的判题反馈边界：始终包含聚合结果与逐用例摘要，只有公开用例才允许附带原始输入、答案输出和用户输出。
_Avoid_: 向用户暴露隐藏判题数据, 只返回总状态

**错误信息可见性**:
错误信息可见性是评测反馈中编译诊断、运行错误输出与异常摘要的暴露边界：试运行可返回原始错误信息，正式提交只对公开语境返回原始错误信息，隐藏语境只返回脱敏摘要。
_Avoid_: 原样回传隐藏用例错误输出, 完全不给调试信息

**后台判题资产可见性**:
后台受权角色可以查看隐藏用例、固定判题数据、判题参考程序与特判程序的原始内容，用于制题、排障与回溯。
_Avoid_: 用户侧暴露敏感判题资产

**异步正式提交**:
异步正式提交会先创建提交记录并返回标识，再由后台完成真实评测并回写最终结果。
_Avoid_: 同步长耗时提交

**提交源码**:
提交源码是用户为某种语言提交的完整可编译可运行程序文本，而不是待宿主调用的函数片段。
_Avoid_: 方法片段, 函数体补全

**语言会话草稿**:
语言会话草稿是用户在某个版本绑定会话中针对某一种支持语言独立保存的可编辑源码草稿，首次创建时由该语言的起始模板初始化，之后互不覆盖。
_Avoid_: 全局唯一会话草稿, 切语言互相覆盖

**单提交单元**:
程序题一次提交只包含一个可独立编译运行的提交单元，通常是一份单文件源码，不包含工程压缩包或多文件项目。
_Avoid_: 项目式提交, 多文件工程提交

**单次编译**:
单次编译是指同一份提交源码在一次评测过程中只进行一次编译，随后复用同一编译产物执行多个用例。
_Avoid_: 每个用例重复编译, 编译行为随用例漂移

**用例级进程隔离**:
用例级进程隔离是指同一份提交源码在同一次评测中，每个用例都在全新的独立运行进程里执行，不共享内存状态、静态变量、临时文件或标准输入游标。
_Avoid_: 多用例复用同一运行进程, 跨用例状态污染

**固定入口约束**:
每种语言的提交源码都遵循平台预先规定的文件名与程序入口约束，而不是由题目或用户自定义入口名。
_Avoid_: 自定义入口名, `functionName` 驱动入口

## Relationships

- **程序题评测契约** 约束 **题目描述** 如何给出输入输出格式
- **程序题评测契约** 约束 **代码提交** 如何被编译、运行和判定
- **题目本体** 可以关联多个 **题目语言资产**
- **题目语言资产** 依附于单个 **题目本体**
- **示例用例** 属于 **题目本体** 的公开表达
- **完整判题集** 服务于 **正式提交**
- **试运行** 默认基于 **示例用例**
- **试运行** 采用 **同步试运行**
- **试运行** 采用 **全量试运行**
- **正式提交** 基于 **完整判题集**
- **题目本体** 声明自身的 **判题方式**
- **判题资产** 依附于 **题目本体** 并服务于其 **判题方式**
- **特判程序** 与 **交互器** 属于 **判题资产**，不属于 **题目语言资产**
- **特判程序** 仅在 **判题方式** 为 SPJ 时存在
- **特判程序** 在题目创建或编辑阶段生成并固化，不随用户提交重新生成
- **示例用例** 与 **完整判题集** 都由一个或多个 **标准流用例** 组成
- **标准流用例** 在单个 **题目版本** 内具有稳定的 **用例序号**
- 当 **判题方式** 为 SPJ 时，**试运行** 与 **正式提交** 都依赖同一份 **特判程序**
- **正式提交** 编译和运行 **提交源码**
- **正式提交** 对同一份 **提交源码** 采用 **单次编译**
- **题目语言资产** 为用户提供 **提交源码** 的起始模板
- **提交源码** 以 **单提交单元** 的形式进入评测
- **试运行** 与 **正式提交** 都采用 **用例级进程隔离**
- **固定入口约束** 约束 **题目语言资产** 如何提供起始模板
- **固定入口约束** 约束 **提交源码** 如何被编译和运行
- **标准比对判题** 与 **特判程序** 互斥
- **标准比对判题** 在答案匹配层面只区分 AC 与 WA
- **标准比对判题** 采用 **最小文本归一化**
- **执行终态** 描述提交在答案比对前是否成功完成编译和运行
- **编译阶段失败** 属于 **执行终态**
- **平台侧失败** 属于 **执行终态**
- 当前程序题采用 **通过制判题**
- **完整判题集** 由 **固定判题数据** 构成
- **完整判题集** 由 **公开用例** 与 **隐藏用例** 共同组成
- **固定判题数据** 持久化保存原始 **答案标准输出**
- **判题包** 固化正式评测所需的 **固定判题数据** 与执行配置引用
- **历史评测保留** 约束 **判题包** 与 **执行环境版本** 的生命周期
- **题目语言资产** 可以包含 **语言参考解答**
- **判题参考程序** 属于 **判题资产**
- **判题参考程序** 在题目制作阶段生成或校验 **固定判题数据**
- **判题参考程序** 采用平台内部单一语言实现
- 当 **判题方式** 为 SPJ 时，**特判程序** 接收 `stdin`、`userStdout`、**答案标准输出** 三类输入
- **特判结果合同** 区分“用户答案错误”和“特判程序自身失败”
- **特判结果合同** 可以附带 **特判判定消息**
- **特判结果合同** 通过 **特判结构化输出** 落地
- **SPJ 自验样例** 用于验证 **特判程序** 是否符合 **特判结果合同**
- **自动发布** 依赖系统校验通过，而不是人工审核
- **自动发布** 不等于 **公共题库上架**
- **发布闸门** 决定题目能否 **自动发布**
- 当前重构范围内，workflow 链路不会修改既有 **题库题目**
- 当前重构范围内，workflow 重新生成题目时会创建新的 **题库题目**
- **题库题目** 承载多个 **题目版本**
- 只有后台受权管理员可以通过 **管理员版本修订** 为既有 **题库题目** 创建新的 **题目版本**
- **题目公开范围** 约束 **题库题目** 对哪些用户可见、可检索和可进入
- **题目公开范围** 属于 **题库题目**，不属于单个 **题目版本**
- 进入 **题目归档** 的 **题库题目** 不再向普通用户开放新会话入口
- **提交记录** 必须绑定其生效时的 **题目版本**
- **提交记录** 必须绑定其实际使用的 **执行环境版本**
- **判题限制** 属于 **题目版本**
- **题目版本** 绑定一个 **默认执行环境版本**
- 每个 **题目版本** 在其 **默认执行环境版本** 下生成并绑定一个 **判题包**
- **执行环境版本** 负责按技术配置执行 **判题限制**
- **语言限制系数** 属于 **执行环境版本**
- 实际生效的资源限制 = **判题限制** × **语言限制系数**
- **提交记录** 对外通过 **最终判定** 表达主状态
- **正式提交** 采用 **短路判题**
- **通过计数** 属于 **提交记录** 的聚合结果
- **提交记录** 在创建时固化一份 **提交快照**
- **试运行** 与 **正式提交** 都沿用其目标 **题目版本** 中已固化的 **用例序号**
- **提交可见明细** 属于 **提交记录** 的用户展示边界
- **错误信息可见性** 约束 **试运行明细** 与 **提交可见明细** 中的错误反馈范围
- **版本绑定会话** 决定用户的 **试运行** 与 **正式提交** 使用哪个 **题目版本**
- **版本绑定会话** 采用 **会话版本固定**
- **版本绑定会话** 可以同时持有多个 **语言会话草稿**
- **历史版本访问边界** 约束哪些 **题目版本** 可以被普通用户新开会话
- **支持语言集** 属于 **题目版本**
- **支持语言集** 必须是其 **默认执行环境版本** 可运行语言集合的子集
- 用户只能对 **支持语言集** 中的语言执行 **试运行** 与 **正式提交**
- **支持语言集** 中的每一种语言都必须通过 **发布闸门**
- AI 制题先生成 **草稿版本**
- AI 制题产物属于 **题库题目** 的生命周期，而不是会话临时题
- 当前重构范围内，单题 AI 制题使用 **平台预设制题语言集**
- **草稿版本** 通过 **发布闸门** 后才会原子切换为对外可用的 **题目版本**
- 未通过 **发布闸门** 的 **草稿版本** 作为 **失败草稿版本** 被保留
- **失败草稿版本** 属于同一 **题库题目** 的 **草稿尝试序列**
- 当前重构范围只提供单题 AI 制题与自动验题，不提供批量出题任务功能
- 单题 AI 制题成功后，触发生成的当前会话自动绑定新生成的 **题目版本**
- 单题 AI 制题失败并停留在 **失败草稿版本** 时，当前会话保持原有 **题目版本** 绑定不变
- AI 自动生成并发布的 **题库题目** 默认不自动进入全站公共检索范围
- **公共题库上架** 需要后台受权角色显式执行
- 对已产生已发布版本或 **提交记录** 的 **题库题目**，当前只允许 **题目归档**，不允许物理删除
- **发布闸门** 要求每个可发布题目版本至少具有一个 **公开用例** 和一个 **隐藏用例**
- 当 **判题方式** 为 SPJ 时，**发布闸门** 必须验证至少一组判 AC 和至少一组判 WA 的 **SPJ 自验样例**
- **发布闸门** 必须验证至少一份 **错误实现样例** 在 **完整判题集** 上不能得到 AC
- **发布闸门** 的所有执行性校验都必须在该 **题目版本** 绑定的 **默认执行环境版本** 上完成
- 新进入题目的会话默认绑定该题当前对外生效的 **题目版本**
- 当前重构范围不对普通用户提供同题历史 **题目版本** 入口
- **题目语言资产** 包含 **起始模板** 与可选的 **语言参考解答**
- 用户首次切换到某个支持语言时，由对应 **起始模板** 初始化该语言的 **语言会话草稿**
- 单题 AI 制题成功并自动绑定会话后，应为每种支持语言初始化对应的 **语言会话草稿**
- **语言资产验收** 是 **发布闸门** 的一部分
- **起始模板** 的验收采用 **全量试运行** 口径
- **起始模板** 在全部 **公开用例** 上都必须稳定结束为 WA，不能出现 AC、CE、TLE、MLE、RE、SE
- **试运行** 只运行 **公开用例**
- **试运行** 返回完整的 **试运行明细**
- **正式提交** 采用 **短路判题**，**试运行** 不采用 **短路判题**
- **正式提交** 运行 **公开用例** 与 **隐藏用例**
- **正式提交** 对用户返回 **提交可见明细**
- 当 **试运行** 发生 **编译阶段失败** 时，不返回 **试运行明细**
- **试运行** 可以返回原始编译诊断与公开用例的原始运行错误输出
- **提交记录** 对用户隐藏 **隐藏用例** 的原始输入与标准输出
- **公开用例** 在 **提交可见明细** 中可以展示原始输入、答案输出与用户输出
- **正式提交** 在 CE 场景下可以返回原始编译诊断
- **正式提交** 如果失败发生在公开用例，可以返回该公开用例的原始错误输出
- **正式提交** 如果失败发生在隐藏用例，只返回脱敏错误摘要，不返回原始错误输出
- **特判判定消息** 在公开语境下可以原样返回，在隐藏语境下必须脱敏
- **隐藏用例** 在 **提交可见明细** 中只展示摘要，不展示原始判题数据
- 当 **正式提交** 发生 **编译阶段失败** 时，不返回逐用例 **提交可见明细**
- 用户程序超出输出限制时归入 **RE**
- **SE** 只用于 **平台侧失败**
- **公开用例** 是按同一套 **用例序号** 暴露给用户的可见子集
- **完整判题集** 采用 **公开前置顺序**
- **提交记录** 只为 **正式提交** 留档
- **后台判题资产可见性** 只授予具备相应权限的后台角色
- **异步正式提交** 会创建并更新 **提交记录**
- **正式提交** 不直接运行 **语言参考解答**
- **正式提交** 只消费已固化的 **判题包**
- 只要已有 **提交记录** 绑定某个 **判题包** 与 **执行环境版本**，它们就受 **历史评测保留** 约束
- **判题参考程序** 不进入 **正式提交** 链路
- 当前重构范围不提供 **重判** 与 **迁移并重判** 功能
- 当前重构范围只包含 **标准比对判题** 与 SPJ，不包含交互判题

## Example dialogue

> **Dev:** “这道题的测试输入是直接传 `functionName` 参数吗？”
> **Domain expert:** “不是，程序题一律按标准输入喂给用户程序，再从标准输出判定结果。”

> **Dev:** “用户点运行和点提交，走的是同一批测试数据吗？”
> **Domain expert:** “不是，**试运行** 只用 **示例用例**，**正式提交** 才跑 **完整判题集**。”

## Flagged ambiguities

- “代码执行” 曾同时指函数调用式自测和标准输入输出式评测; resolved: 当前程序题统一采用 **程序题评测契约**，即标准输入输出式评测。
- “题目” 曾同时承载语言无关题意和语言相关模板; resolved: 语言无关内容属于 **题目本体**，语言相关代码内容属于 **题目语言资产**。
- “运行” 曾可能同时指试跑和正式判题; resolved: 面向用户的即时验证称为 **试运行**，产出最终结果的流程称为 **正式提交**。
- “试运行返回方式” 曾可能与正式提交一样走异步留档; resolved: 当前 **试运行** 采用 **同步试运行**，直接返回结果且不写入 **提交记录**。
- “试运行是否遇错即停” 曾可能不明确; resolved: 当前 **试运行** 采用 **全量试运行**，即使出现 WA、RE、TLE 等结果也继续执行剩余公开用例。
- “代码生成内容” 曾可能混入判题逻辑; resolved: **判题资产** 单独建模，`特判程序` 不属于 **题目语言资产**。
- “特判逻辑” 曾可能被理解为随代码生成节点反复生成; resolved: **特判程序** 是题目创建阶段固化的 **判题资产**。
- “测试用例” 曾使用对象参数和函数返回值建模; resolved: 程序题统一采用 **标准流用例**。
- “用户代码” 曾被当成可反射调用的方法; resolved: 用户提交的是完整 **提交源码**。
- “一次提交” 曾可能被理解为多文件项目; resolved: 当前程序题提交受限于 **单提交单元**。
- “多用例执行是否共享运行态” 曾可能不明确; resolved: 当前 **试运行** 与 **正式提交** 都采用 **用例级进程隔离**，同一评测中的每个用例都在独立进程运行。
- “多用例是否重复编译” 曾可能不明确; resolved: 当前同一份 **提交源码** 在单次评测中采用 **单次编译**，随后复用编译产物执行各用例。
- “程序入口” 曾由函数名隐式决定; resolved: 当前程序题采用 **固定入口约束**。
- “判题方式” 曾可能扩展到交互题; resolved: 当前重构范围不包含交互判题。
- “格式错误” 曾可能被单独判为 PE; resolved: 当前 **标准比对判题** 不引入 PE，答案匹配只产生 AC 或 WA。
- “标准输出比对” 曾可能混入 `trim`、token 级比较或无序容忍; resolved: 当前 **标准比对判题** 只允许 **最小文本归一化**，更宽松的语义必须显式使用 **特判程序**。
- “编译失败后是否要生成逐用例结果” 曾可能不明确; resolved: 当前 **编译阶段失败** 发生在进入任何用例之前，因此 **试运行** 与 **正式提交** 都不生成逐用例明细。
- “输出超限应算用户错误还是系统错误” 曾可能不明确; resolved: 当前用户程序超出输出限制归入 **RE**，而 **SE** 只保留给 **平台侧失败**。
- “失败结果” 曾可能全部压成 WA; resolved: 编译或运行失败属于独立 **执行终态**。
- “正式提交结果” 曾可能扩展为分值体系; resolved: 当前程序题采用 **通过制判题**，不做部分分与总分。
- “标准答案” 曾可能依赖参考解答运行时生成; resolved: 当前程序题的正式提交使用固化的 **固定判题数据**。
- “标准答案存储” 曾可能只保留 md5 等摘要; resolved: 当前 **固定判题数据** 必须保存原始 **答案标准输出**，哈希最多作为派生校验值，不能替代原文。
- “正式提交运行时是否临时拼装判题资产” 曾可能不明确; resolved: 当前 **正式提交** 只消费已固化的 **判题包**，不在运行时临时拼装多份判题资产。
- “历史提交对应的判题包和运行环境是否可以被回收” 曾可能不明确; resolved: 当前只要已有 **提交记录** 绑定某个 **判题包** 与 **执行环境版本**，它们就受 **历史评测保留** 约束，必须持续可用。
- “测试用例顺序” 曾可能只是实现细节; resolved: 当前 **用例序号** 属于 **题目版本** 语义，发布后在同版本内不可重排，公开用例只是其中可见子集。
- “公开用例和隐藏用例是否可以交错排序” 曾可能不明确; resolved: 当前 **完整判题集** 采用 **公开前置顺序**，所有公开用例必须先于隐藏用例。
- “SPJ 判定” 曾可能只在正式提交时生效; resolved: 当题目采用 SPJ 时，**试运行** 与 **正式提交** 共用同一份 **特判程序**。
- “示例用例” 与 “判题集” 曾可能被建成两套独立数据; resolved: 当前程序题采用统一用例池，由 **公开用例** 和 **隐藏用例** 组成。
- “运行记录” 曾可能与正式提交混用; resolved: **提交记录** 只对应 **正式提交**，不包含 **试运行**。
- “正式提交” 曾可能同步阻塞请求; resolved: 当前程序题采用 **异步正式提交**。
- “referenceAnswer” 曾同时指语言示例解和判题真相源; resolved: 面向用户与 AI 的解法属于 **语言参考解答**，制题期生成判题数据的程序属于 **判题参考程序**。
- “SPJ 输入” 曾可能只包含用户输出; resolved: 当前 **特判程序** 采用 `stdin + userStdout + 答案标准输出` 三输入合同。
- “SPJ 输出” 曾可能只靠退出码表达; resolved: 当前 **特判结果合同** 使用结构化标准输出表达业务判定。
- “SPJ 的 WA 原因是否可以直接回给用户” 曾可能不明确; resolved: 当前允许返回简短 **特判判定消息**，但它必须受 **错误信息可见性** 约束，不能泄露隐藏判题数据。
- “SPJ 成功输出能否自定义格式” 曾可能不明确; resolved: 当前 **特判结构化输出** 固定为只包含 `verdict` 与可选 `message` 的 JSON 对象，其他业务字段不进入合同。
- “SPJ 是否只能靠人工确认正确” 曾可能不明确; resolved: 当前 SPJ 题必须提供 **SPJ 自验样例** 并纳入 **发布闸门**，至少覆盖一个应判 AC 和一个应判 WA 的样例。
- “样例和隐藏用例是否可以缺省其一” 曾可能不明确; resolved: 当前 **发布闸门** 要求每个可发布题目版本至少同时具有一个 **公开用例** 和一个 **隐藏用例**。
- “自动验题是否只要证明正确解能过” 曾可能不明确; resolved: 当前 **发布闸门** 还必须验证至少一份 **错误实现样例** 在 **完整判题集** 上不能得到 AC，以证明判题具备拦错能力。
- “题目发布” 曾被假定需要人工审核; resolved: 当前题目采用 **自动发布**，不依赖人工审核。
- “自动发布是否等于自动进入全站公共题库” 曾可能不明确; resolved: 当前 **自动发布** 只表示题目版本可用，不等于 **公共题库上架**，AI 生成题默认不自动进入全站公共检索范围。
- “题目公开范围应挂在题目层还是版本层” 曾可能不明确; resolved: 当前 **题目公开范围** 属于 **题库题目**，而不是单个 **题目版本**。
- “有历史记录的题目是否可以物理删除” 曾可能不明确; resolved: 当前对已产生已发布版本或 **提交记录** 的 **题库题目** 只允许 **题目归档**，不允许物理删除。
- “发布前校验” 曾可能依赖人工把关; resolved: 当前题目通过 **发布闸门** 做系统化自动校验。
- “已发布题目修改” 曾可能原地覆盖历史语义; resolved: 当前 workflow 链路不会修改既有 **题库题目**，但后台受权管理员可以通过 **管理员版本修订** 为同一 **题库题目** 创建新的 **题目版本**。
- “同一道题是否支持多个已发布版本” 曾可能不明确; resolved: 当前同一 **题库题目** 可以承载多个 **题目版本**，但普通用户默认只进入当前对外生效版本。
- “AI 生成题目” 曾可能只存在于当前会话; resolved: 当前 AI 制题产物属于 **题库题目**，先形成 **草稿版本**，再经 **发布闸门** 发布，而不是会话临时题。
- “AI 出题未通过发布闸门时是否直接丢弃” 曾可能不明确; resolved: 当前未通过 **发布闸门** 的 AI 产物会作为 **失败草稿版本** 保留，并附带结构化失败原因以支持后续修复与重试。
- “AI 出题失败后是否覆盖上一次失败现场” 曾可能不明确; resolved: 当前同一 **题库题目** 下的失败与修复尝试形成 **草稿尝试序列**，每次尝试都保留为独立草稿版本。
- “是否实现 AI 批量出题任务功能” 曾可能不明确; resolved: 当前重构范围只提供单题 AI 制题与自动验题，不提供批量出题任务功能。
- “单题 AI 制题成功后是否还需要用户手动重新选题” 曾可能不明确; resolved: 当前单题 AI 制题成功后，会自动把触发生成的会话绑定到新 **题目版本**，并初始化各语言 **语言会话草稿**。
- “单题 AI 制题失败后是否把会话切到失败产物上” 曾可能不明确; resolved: 当前单题 AI 制题失败并停留在 **失败草稿版本** 时，当前会话保持原有 **题目版本** 绑定不变，只返回结构化失败摘要。
- “新版本发布后用户正在做题的会话” 曾可能自动切到最新版; resolved: 当前 **版本绑定会话** 采用 **会话版本固定**，新版本发布不会让既有会话自动漂移，只有新进入题目的会话默认绑定当前对外生效版本。
- “普通用户是否能随意回到旧题义” 曾可能不明确; resolved: 当前受 **历史版本访问边界** 约束，普通用户默认只能新开最新已发布版本，历史版本主要用于既有会话、提交记录与后台追溯。
- “用户提交跑哪个版本” 曾可能隐式跟随最新题目; resolved: 当前通过 **版本绑定会话** 显式绑定题目版本。
- “历史提交的源码是否需要固化留档” 曾可能不明确; resolved: 当前 **提交记录** 创建时固化 **提交快照**，用于后续评测结果追溯与后台核查。
- “同一会话里切换语言是否共用一份代码” 曾可能不明确; resolved: 当前每个 **版本绑定会话** 为每种支持语言维护独立的 **语言会话草稿**，首次由对应 **起始模板** 初始化。
- “题目支持哪些语言” 曾可能来自全局枚举而非题目自身; resolved: 当前由题目版本上的 **支持语言集** 显式声明。
- “单题 AI 制题的语言范围是否由会话临时决定” 曾可能不明确; resolved: 当前重构范围内单题 AI 制题使用平台统一配置的 **平台预设制题语言集**，而不是会话临时选语言。
- “可用语言” 曾可能只要求其中一门可运行; resolved: 当前 **支持语言集** 中的每一种语言都必须通过 **发布闸门**。
- “AI 制题过程” 曾可能边生成边对外可见; resolved: 当前先生成不可见的 **草稿版本**，通过校验后原子发布。
- “运行环境” 曾可能隐式跟随全局最新配置; resolved: 当前通过 **执行环境版本** 显式追踪编译与运行环境。
- “用户试运行和正式提交默认跑哪个环境” 曾可能不明确; resolved: 当前每个 **题目版本** 绑定一个 **默认执行环境版本**，用户侧默认都使用它。
- “题目声明支持的语言是否可以超出默认运行环境能力” 曾可能不明确; resolved: 当前 **支持语言集** 必须是其 **默认执行环境版本** 可运行语言集合的子集。
- “发布时在哪个环境上做验收” 曾可能不明确; resolved: 当前 **发布闸门** 的执行性校验必须在该 **题目版本** 绑定的 **默认执行环境版本** 上完成。
- “是否需要提供重判或迁移并重判功能” 曾可能不明确; resolved: 当前重构范围不提供 **重判** 与 **迁移并重判**，只保留历史结果追溯所需的版本绑定与资产留存。
- “资源限制” 曾可能被视为环境默认值; resolved: 当前由 **题目版本** 显式声明 **判题限制**，再由 **执行环境版本** 执行。
- “所有语言同一绝对限制” 曾可能造成不公平; resolved: 当前通过 **语言限制系数** 在执行环境层做语言差异调整。
- “executeStatus” 曾同时混合执行与结果语义; resolved: 当前提交记录对外统一使用 **最终判定**。
- “正式提交是否继续跑完整判题集” 曾可能不明确; resolved: 当前 **正式提交** 采用 **短路判题**。
- “passCount / totalCount” 曾可能被理解为已执行数量; resolved: 当前 **通过计数** 中 `totalCount` 表示完整判题集总数。
- “codeSkeleton” 曾指不可独立执行的方法骨架; resolved: 当前对用户暴露的是完整程序级 **起始模板**。
- “起始模板可运行” 曾可能只意味着不报编译错; resolved: 当前 **起始模板** 的验收采用 **全量试运行** 口径，要求其在全部 **公开用例** 上稳定得到 WA，不能出现 AC 或任何执行失败终态。
- “试运行结果” 曾可能只返回整体成败; resolved: 当前 **试运行** 返回逐个公开用例的 **试运行明细**。
- “提交详情” 曾可能泄露完整判题集; resolved: 用户视角的 **提交记录** 不暴露 **隐藏用例** 原始数据。
- “正式提交反馈” 曾可能要么过粗、要么泄露隐藏数据; resolved: 当前 **提交可见明细** 始终提供聚合结果与逐用例摘要，但只有 **公开用例** 才展示原始输入输出。
- “错误输出是否会把隐藏判题数据原样带给用户” 曾可能不明确; resolved: 当前受 **错误信息可见性** 约束，**试运行** 可返回原始错误信息，但 **正式提交** 在隐藏用例失败时只返回脱敏摘要。
- “隐藏判题资产” 曾可能对所有查看者一视同仁; resolved: 当前仅后台受权角色具备 **后台判题资产可见性**。
- “判题参考程序” 曾可能随支持语言各自产生一份; resolved: 当前 **判题参考程序** 固定为平台内部单一语言实现。
